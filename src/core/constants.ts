/** Shared engine constants. Internal render resolution + simulation timestep. */
export const INTERNAL_W = 480;
export const INTERNAL_H = 270;

/** Tile edge length in internal pixels. All Kenney tiles are 18x18. */
export const TILE = 18;

/** Fixed simulation step: 60 updates per second. */
export const FIXED_STEP = 1 / 60;
